"""Business rules layer.

Knows nothing about HTTP or SQL details directly — depends on the
repository layer. `base.py` has the generic `BaseService` shared by
every entity; no domain service yet, will be added in future steps.
"""
